# Authentication using the RTSP protocol in the format "username:password"
RTSP_AUTH="admin:admin"

# Flask settings (default values are specified)
FLASK_DEBUG=false
SECRET_KEY="change-this-secret-key-in-production"
HOST=0.0.0.0
PORT=5000
